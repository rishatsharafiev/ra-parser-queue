# ra-parser-queue

### docker-compose
Start services
```
docker-compose up -d --scale chrome=5
```

Scale chrome nodes
```
docker-compose scale chrome=5
```

Down services
```
docker-compose down
```
